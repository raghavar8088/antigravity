# EXECUTIVE SUMMARY — Phase 19
Generated: 2026-06-11 | Forensic Code Audit

---

## TOP 25 ARCHITECTURE PROBLEMS

| # | Problem | File:Line | Severity |
|---|---|---|---|
| A-1 | Terminal dashboard (`/terminal/*`) permanently shows hardcoded demo data. `NEXT_PUBLIC_TERMINAL_WS_URL` is not set in `.env.local`, so `terminalStore.tsx:22` exits WebSocket setup. All 5 terminal pages (execution, risk, analytics, research, journal) display static snapshot from `terminalSnapshot.ts`. | `client/src/lib/terminal/terminalStore.tsx:21-22`, `client/.env.local` | CRITICAL |
| A-2 | Dual trading engine problem: The Go engine (canonical) and the client-side mock engine (`useMockTradingEngine`) are parallel systems writing to different MongoDB collections (`paper_trades` vs mock collections). Users trade on both systems simultaneously with no clear separation in the UI. | `client/src/hooks/useMockTradingEngine.ts:218-465`, `engine/internal/trading/loop.go:79-175` | HIGH |
| A-3 | Paper Desk uses polling (5s interval) for all real-time data. The Go engine snapshots state every 10s. Net latency from position open to UI visibility is up to 15s. For a paper trading engine that executes in milliseconds, the UI is structurally stale. | `client/src/hooks/usePaperDesk.ts:34`, `engine/internal/paperpersist/state_snapshotter.go:89` | HIGH |
| A-4 | The reconciliation engine (`reconciliationv2`) runs in-process but results never reach the UI. The audit log writes to `ledger.NewMemoryStore()` (in-process memory) not MongoDB, so all reconciliation events are lost on engine restart and invisible to dashboard users. | `engine/internal/trading/loop.go:235`, `engine/internal/reconciliationv2/wiring.go:51` | HIGH |
| A-5 | BTC mark price is sourced from two different paths: engine uses WS/REST from Delta/Binance directly; UI uses Binance REST → Coinbase fallback via `/api/btc/price`. These can diverge, causing UI unrealized PnL to differ from engine's authoritative calculation. | `engine/internal/trading/paperpersist_hooks.go:139`, `client/src/app/api/btc/price/route.ts:14-29` | HIGH |
| A-6 | `ledger.NewMemoryStore()` used for OMS v3 event ledger in production. The ledger is the event source of truth for order/position lifecycle (used by reconciliation, kill switch restore, crash recovery). Restarting the engine loses all in-flight OMS events. | `engine/internal/trading/loop.go:235` | HIGH |
| A-7 | Strategy enable/disable state is hardcoded in `BuildCuratedScalpers()`. There is no runtime way to disable a specific strategy without redeploying the engine. The UI shows strategy health but cannot act on it. | `engine/internal/strategy/curated_registry.go:6` | HIGH |
| A-8 | The `portfolioConsistencyValidation.ts` PnL tolerance is `$50 USD`. This means up to $50 of realized PnL drift between `paper_state` and `SUM(paper_trades.net_pnl)` passes silently as a PASS. | `client/src/lib/portfolioConsistencyValidation.ts:33` | MED |
| A-9 | Multiple duplicate "starting balance" constants exist: `futuresInitialCapitalUSD = 1000000.0` (Go loop.go:33), `initialPaperBalanceUSD = 1000000.0` (main.go:76), `PAPER_DESK_STARTING_BALANCE` (TypeScript), and `balance: 1_000_000` hardcoded in state fallback (state/route.ts:44). If any diverge, accounting breaks silently. | `engine/cmd/antigravity/main.go:76`, `engine/internal/trading/loop.go:33`, `client/src/app/api/paper-desk/state/route.ts:44` | MED |
| A-10 | No data version or schema version negotiation between Go engine and Next.js MongoDB reader. If the engine evolves a document shape, TypeScript interfaces in `paperDeskClient.ts` silently return `undefined` fields which display as "—". | `client/src/lib/paperDeskClient.ts:30-175` | MED |
| A-11 | The `CommandCenter` component fetches from Go engine directly via `resolveEngineApiUrl()` without authentication. Any CORS-permissive deployment would expose internal engine state. | `client/src/components/CommandCenter.tsx:55,65` | MED |
| A-12 | `useMockCandleBuilder` builds OHLCV candles client-side from a live price stream using an in-memory state machine. These are "invented" candles, not exchange candles. The dashboard's market regime detection (`useMarketRegime`) and the "Candles Built" status counter on the root dashboard are based on these synthetic candles. | `client/src/components/TerminalDashboard.tsx:258` | MED |
| A-13 | The engine proxy (`/api/engine/[...path]/route.ts`) has a `DENIED` default that blocks any unlisted engine route. When new engine routes are added, they are silently 403'd until the allowlist is updated. No automated test verifies the allowlist covers all used routes. | `client/src/app/api/engine/[...path]/route.ts:99-113` | MED |
| A-14 | Vercel 60s function timeout creates reconnect storms on NIFTY SSE. The stream route uses `maxDuration = 60` and clients reconnect automatically, but between the 60s stream end and reconnect, NIFTY price data is stale. No client-side indicator of SSE reconnect. | `client/src/app/api/nifty/stream/route.ts:12` | MED |
| A-15 | `paper_state` MongoDB document is a singleton upsert per account, not an append-only log. If the engine writes a corrupted snapshot, there is no recovery — the previous snapshot is overwritten. No write-through audit log for state changes. | `engine/internal/paperpersist/state_snapshotter.go:161-162` | MED |
| A-16 | `portfolio_ledger.go` tracks realized PnL in-memory as an additive counter. On engine restart, `PortfolioLedger` is reconstructed from `paper_trades` via a MongoDB aggregate query. The aggregate query at line 112 uses `$ifNull: ["$net_pnl", 0]` — if a trade document has a corrupt `net_pnl`, it is silently zeroed and lost from the ledger. | `engine/internal/paperpersist/portfolio_ledger.go:112` | MED |
| A-17 | NIFTY options chain returns a synthetic (hardcoded) option chain when Angel One API fails. The warning is logged server-side but the response returned to the client carries no indication it is synthetic. | `client/src/app/api/nifty/option-chain/route.ts:331` | MED |
| A-18 | The AngelOne direct order route is blocked/retired (`/api/angelone/order`) but there is no `/api/execution/request` route defined in the client code. If something calls this path, it will 404. | `client/src/app/api/angelone/order/route.ts:6-9` | MED |
| A-19 | `mockTradingMongo.ts:278` uses `p.catch(() => {})` on a bulk write array. A failed trade write returns success to the calling hook. The mock trading hook believes the trade was saved when it was not. | `client/src/lib/mockTradingMongo.ts:278` | MED |
| A-20 | The `usePaperDesk` hook has only two failure modes: "stale" (keeps last data) and "error" (same). There is no timeout after which stale data is cleared. A user could stare at 24-hour-old data with only a yellow "Stale" badge as indication. | `client/src/hooks/usePaperDesk.ts:104-106` | MED |
| A-21 | The `ExecutionWatchdog` and `PortfolioLedger` are components of the Go engine but their health metrics are not exposed in the Paper Desk snapshot API response. The UI has no way to detect if the execution watchdog has fired. | `engine/internal/trading/loop.go:170-172` | MED |
| A-22 | The live BTC price feed (`useLiveBTCPrice`) does not expose a feed age. If Binance and Coinbase both return stale data (e.g. during an exchange maintenance window), the hook returns `connected: true` with a stale price because the HTTP call succeeds (200 OK with cached data). | `client/src/hooks/useLiveBTCPrice.ts:37-69` | LOW-MED |
| A-23 | Research Center hardcoded walk-forward percentages (`74 - i * 7`%) for 4 historical folds are mathematically decreasing by construction and bear no relation to actual backtested out-of-sample performance. | `client/src/components/terminal/institutional/ResearchCenter.tsx:59` | LOW |
| A-24 | The `equitySparkline` in `PaperDeskDashboard.tsx` only fetches 288 equity points over 30 days maximum. At 1-minute snapshots (engine default), this is 288 minutes = 4.8 hours of data, not 30 days. The parameter naming is misleading. | `client/src/hooks/usePaperDesk.ts:177` | LOW |
| A-25 | No circuit breaker or backpressure on the polling loop in `usePaperDesk`. If the MongoDB query takes >5s, the next poll fires before the previous completes. The `AbortController` mitigates this but there is no exponential backoff on repeated failures. | `client/src/hooks/usePaperDesk.ts:80-106` | LOW |

---

## TOP 25 UI PROBLEMS

| # | Problem | File:Line |
|---|---|---|
| U-1 | Terminal execution page shows fake BTC price `$105,842.5`, hardcoded into `terminalSnapshot.ts:19`. The live WebSocket path is disabled. | `client/src/lib/terminal/terminalSnapshot.ts:19` |
| U-2 | Terminal risk page VaR/CVaR/heat metrics are hardcoded constants from snapshot (`var95Usd: 1_840` etc.), displayed as if live. | `client/src/lib/terminal/terminalSnapshot.ts:67-79` |
| U-3 | "Exposure By Family" panel (Risk page) shows 4 hardcoded families with static percentages regardless of which strategies are running. | `client/src/components/terminal/institutional/RiskModule.tsx:7-12` |
| U-4 | Correlation heatmap on Risk page uses formula `0.82 - Math.abs(rowIdx - colIdx) * 0.18` — not real correlations. | `client/src/components/terminal/institutional/RiskModule.tsx:38-42` |
| U-5 | Trade Journal on `/terminal/journal` shows exactly 3 trades hardcoded (IDs T-8831, T-8838, T-8842). No live data. | `client/src/lib/terminal/terminalSnapshot.ts:111-113` |
| U-6 | Analytics Center equity curve chart has only 6 hardcoded time points (09:15 to 14:15). The SVG rendering logic (`40 + i * 120`) would be clipped for large datasets. | `client/src/components/terminal/institutional/AnalyticsCenter.tsx:19` |
| U-7 | Strategy leaderboard on Research Center shows 5 hardcoded strategies from snapshot. The actual engine runs 600+ strategies — none are shown. | `client/src/lib/terminal/terminalSnapshot.ts:81-87` |
| U-8 | Research Tournament panel shows hardcoded `Active Pool: 64`, `Eligible: 11`, `Retired: 7` — these are static and meaningless. | `client/src/components/terminal/institutional/ResearchCenter.tsx:48-51` |
| U-9 | Walk-forward validation shows 4 folds with percentages `74%, 67%, 60%, 53%` — a linear sequence with no basis in real data. | `client/src/components/terminal/institutional/ResearchCenter.tsx:56-65` |
| U-10 | BTC price on root dashboard uses `useLiveBTCPrice` which polls every 3s. If price fetch fails, the last price stays visible indefinitely. The "LIVE" badge on the dashboard remains green. | `client/src/components/TerminalDashboard.tsx:97-123`, `client/src/hooks/useLiveBTCPrice.ts:66-67` |
| U-11 | "Candles Built" counter in System Status panel shows count from `useMockCandleBuilder` — these are synthetic candles built from polling, not real exchange candles. | `client/src/components/TerminalDashboard.tsx:632-634` |
| U-12 | When `connection === "unauthorized"` the root dashboard shows "Sign in to load Paper Desk trade history" but does not provide a sign-in link in the leaderboard panel. | `client/src/components/TerminalDashboard.tsx:481-485` |
| U-13 | "Active Strategies" count on the root dashboard defaults to `strategyScores.length` (from engine MongoDB health data). If engine has never written strategy scores, this shows `0` even though 600+ strategies may be running. | `client/src/components/TerminalDashboard.tsx:636-638` |
| U-14 | NIFTY option chain returns synthetic data on Angel One failure with no client-side indicator. Users believe they see real option chain. | `client/src/app/api/nifty/option-chain/route.ts:331` |
| U-15 | `Paper Desk` `connection = "stale"` shows last-known data indefinitely. No timeout or automatic "data is X minutes old" warning. | `client/src/hooks/usePaperDesk.ts:104-105` |
| U-16 | The `KpiCard` on root dashboard shows `Win Rate: 0%` and `0 total trades` when Paper Desk is not connected. This is misleading — users may think their account has no trades. | `client/src/components/TerminalDashboard.tsx:429-434` |
| U-17 | The signal feed on the root dashboard falls back to `desk.recentTrades` (up to 20 items from snapshot) which are closed trades, not recent signals. The feed title says "Latest engine events" but shows trade closures. | `client/src/components/TerminalDashboard.tsx:350-363` |
| U-18 | Paper Desk unrealized PnL is shown as `$0.00` when live BTC price is unavailable. No indicator that the `0` is due to missing mark price vs an actual flat position. | `client/src/lib/paperDeskPositionMath.ts:19` |
| U-19 | "Open Positions" table on root dashboard only shows first 5 positions (`openPositions.slice(0, 5)`). Users with more than 5 open positions see a truncated view with no clear "more" indicator. | `client/src/components/TerminalDashboard.tsx:365-368` |
| U-20 | Terminal alert panel shows 3 hardcoded alerts (A-1: CRITICAL "Drawdown Guard", A-2: WARNING "Funding Spike", A-3: INFO "Trade Opened"). These are static test data. | `client/src/lib/terminal/terminalSnapshot.ts:115-119` |
| U-21 | Quick Trade Panel on the terminal execution page likely shows hardcoded controls since it derives from the snapshot. No validation that a trade submitted through it would reach a real execution path. | `client/src/components/terminal/institutional/ExecutionCenter.tsx:79+` |
| U-22 | The Paper Desk positions table shows unrealized PnL calculated from BTC price. The BTC price has a 3-second poll interval but the positions list has a 5-second poll interval. A rapidly moving market shows inconsistent unrealized PnL values. | `client/src/hooks/useLiveBTCPrice.ts:17`, `client/src/hooks/usePaperDesk.ts:34` |
| U-23 | The Paper Desk `connection === "connecting"` state has no timeout. If MongoDB is permanently unconfigured, the UI stays "connecting" forever rather than transitioning to "error". | `client/src/hooks/usePaperDesk.ts:72-73` |
| U-24 | Strategy leaderboard on root dashboard uses `s.expectancy` field as the Sharpe ratio column display. Expectancy and Sharpe are different metrics — this mislabels the column. | `client/src/components/TerminalDashboard.tsx:326` |
| U-25 | No CSP headers configured — `NEXT_PUBLIC_TERMINAL_WS_URL` if set allows arbitrary WebSocket connections. No validation that the WS URL points to the expected engine. | `client/src/lib/terminal/terminalStore.tsx:27-32` |

---

## TOP 25 BACKEND PROBLEMS

| # | Problem | File:Line |
|---|---|---|
| B-1 | `ledger.NewMemoryStore()` used for OMS v3 events in production. All order lifecycle events (open, fill, close, cancel) are lost on engine restart. | `engine/internal/trading/loop.go:235` |
| B-2 | `paper_state` singleton document is overwritten every 10s with no version history. A corrupted write destroys the previous snapshot with no recovery path. | `engine/internal/paperpersist/state_snapshotter.go:161-162` |
| B-3 | `computeUnrealizedPnL` uses `o.exec.GetLastPrice()` which returns the last tick price. If no ticks arrive for 5+ minutes (data feed outage), unrealized PnL freezes at a stale mark. The snapshot continues to write the stale value. | `engine/internal/trading/paperpersist_hooks.go:139` |
| B-4 | Strategy health computation runs every 15 minutes (`engine/internal/paperpersist/strategy_health.go`). In fast-moving markets, a strategy that blows up during a 14-minute window will show HEALTHY for the duration. | `engine/internal/paperpersist/strategy_health.go:35` |
| B-5 | `minSampleSize = 20` means strategies with fewer than 20 trades show `INSUFFICIENT_DATA` status. During engine startup or after a reset, ALL strategies show `INSUFFICIENT_DATA` which may be mistaken for "strategy disabled". | `engine/internal/paperpersist/strategy_health.go:37` |
| B-6 | The kill switch `RestoreFromLedger` reads from `s.ledger` which is `ledger.NewMemoryStore()`. On engine restart, the memory store is empty, so the kill switch always starts in `active = false` regardless of whether it was triggered before the restart. | `engine/internal/killswitch/service.go:68-90`, `engine/internal/trading/loop.go:235` |
| B-7 | Reconciliation v2 paper adapter (`paper_reconciliation.go:24`) always returns `nil` for `HealthCheck()`. Exchange health is never reported to the reconciliation cycle for the paper account. | `engine/internal/reconciliationv2/paper_reconciliation.go:24` |
| B-8 | The `portfolio_metrics_writer.go` exists in `paperpersist` package but is not verified to write data that the UI actually reads. The `portfolio_metrics` collection is not queried by any Next.js API route found in the audit. | `engine/internal/paperpersist/portfolio_metrics_writer.go` |
| B-9 | `equity_recorder.go` has a `RecordSnapNow` method but it appears to not be called after every trade close. If it is only called on the 1-minute timer, the equity curve lags behind closed trades. | `engine/internal/paperpersist/equity_recorder.go:119` |
| B-10 | `journal_bootstrap.go` exists in `paperpersist` package — purpose not verified in this audit. If it injects synthetic journal entries on startup, it could contaminate the paper_trades collection. | `engine/internal/paperpersist/journal_bootstrap.go` |
| B-11 | `order_writer.go` in paperpersist writes OMS transitions to `paper_orders` collection. The write uses fire-and-forget goroutines (inferred from the bundle pattern). Failed order writes silently drop audit events. | `engine/internal/paperpersist/order_writer.go` |
| B-12 | The `loadDotEnv` function in `main.go` reads a `.env` file from `../../.env` (two levels above `engine/cmd/antigravity/`). This is the repo root. Credentials in this file are not gitignored in the root `.gitignore` by default. | `engine/cmd/antigravity/main.go:106` |
| B-13 | `PaperReconciliationAdapter` mirrors OMS as both "exchange" and "internal" state. This means reconciliation always reports 0 drift on the paper account — defeating the purpose of reconciliation for detecting OMS-level accounting errors. | `engine/internal/reconciliationv2/paper_reconciliation.go:12-79` |
| B-14 | `fundingCache` in `InstitutionalAlphaScalper` loads from a hardcoded file path `"data/alpha/funding.ndjson"`. If this file doesn't exist on the deploy server, the `NewCache` call may silently fail (returns `(nil, err)` pattern inferred). Funding strategies would run with nil cache. | `engine/internal/strategy/alpha_strategies.go:59` |
| B-15 | The engine self-pings `/health` every 2 minutes to prevent sleep on the hosting provider. The comment references "Render" but the deployment is AWS Lightsail per CLAUDE.md. This self-ping is unnecessary overhead on Lightsail. | `engine/cmd/antigravity/main.go` (referenced in CLAUDE.md) |
| B-16 | The `omsv3` package uses `replay.go` and `replay_engine.go` to reconstruct state. However, the underlying `ledger.Store` is in-memory, so replay only works within a single process lifetime. A crash discards all replay-able events. | `engine/internal/omsv3/replay.go`, `engine/internal/trading/loop.go:235` |
| B-17 | Strategy scoring (`strategy_health.go`) computes `expectancy` as `avg NetPnL per trade`. For strategies with rare large wins and many small losses (typical scalping), expectancy can appear positive while the strategy is operationally losing after fees in the current market regime. | `engine/internal/paperpersist/strategy_health.go:54-65` |
| B-18 | The `RingLogger` in `main.go` keeps the last 100 log lines with no timestamps beyond the line content. Debugging a crash requires the process to be running to retrieve logs. | `engine/cmd/antigravity/main.go:48-74` |
| B-19 | `GetEquityUSD()` on the paper execution client likely derives equity from `balance + unrealized_pnl`. If unrealized PnL calculation uses stale mark price, `GetEquityUSD()` returns a stale equity value written to `paper_state.equity`. | `engine/internal/trading/paperpersist_hooks.go:129` |
| B-20 | The `deltaProbeClient` global variable in `main.go` is a probe-only client. If the full Delta bridge (`deltaBroker *delta.Bridge`) fails to initialize, the probe client may still succeed, misleading health checks. | `engine/cmd/antigravity/main.go:79` |
| B-21 | `recovery.go` in `paperpersist` performs crash recovery by reading open positions from MongoDB. If a position was opened but not yet written to MongoDB before the crash (write latency), it will be lost on recovery. | `engine/internal/paperpersist/recovery.go` |
| B-22 | NSE/BSE market hours gate is managed at the strategy level via `NewOpeningRangeBreakoutScalper(16, 0, 15)` parameters. There is no global session gate that blocks all Indian market strategies outside hours. A misconfigured strategy could trade outside market hours. | `engine/internal/strategy/curated_registry.go:23` |
| B-23 | `buildCuratedScalpers` returns a flat list of ~100+ strategies. At 1m tick intervals, all strategies are evaluated per candle. CPU impact is not measured in the code. There is no strategy-level execution budget or time-slice guard. | `engine/internal/strategy/curated_registry.go:6` |
| B-24 | The `maxSignalStopLossPct = 0.20` constant means SL can be placed 20% below entry price. For a $100K BTC position of 0.01 BTC ($1K value), this is acceptable. But the constant is hardcoded per-signal; it does not scale with portfolio heat or current volatility. | `engine/internal/trading/loop.go:46` |
| B-25 | `validatePhase22e` and `validation/production` packages are imported in `main.go` but their execution results are not surfaced in any MongoDB collection or API response. Validation failures are invisible to the operator UI. | `engine/cmd/antigravity/main.go:44-45` |

---

## TOP 25 SYNCHRONIZATION PROBLEMS

| # | Problem | File:Line |
|---|---|---|
| S-1 | Engine snapshots `paper_state` every 10s; UI polls every 5s. Minimum data age = 10s; maximum = 15s (poll lands just after write). No mechanism to detect or report staleness age. | `engine/internal/paperpersist/state_snapshotter.go:88-90`, `client/src/hooks/usePaperDesk.ts:34` |
| S-2 | Unrealized PnL calculated at two different times: engine calculates at snapshot time (10s interval) using engine mark price; UI recalculates from `paper_positions` at query time using a different price source. These two values can and do diverge. | `engine/internal/trading/paperpersist_hooks.go:139-140`, `client/src/lib/portfolioAccountingService.ts` |
| S-3 | The kill switch state (active/inactive) lives in `killswitch.Service` in-memory. It is not persisted to MongoDB. If the kill switch fires and the engine restarts, it comes back up without the kill switch active. | `engine/internal/killswitch/service.go:52-59`, `engine/internal/trading/loop.go:235` |
| S-4 | The OMS v3 ledger events drive reconciliation, kill switch restore, and position replay. The ledger store is `ledger.NewMemoryStore()`. Every engine restart = full reconciliation state reset. | `engine/internal/trading/loop.go:235` |
| S-5 | Strategy health is computed from closed trades every 15 minutes. The UI shows this health status as-is. If a strategy generates 10 winning trades then 20 losing trades inside a 15-minute window, it shows HEALTHY for the entire window. | `engine/internal/paperpersist/strategy_health.go:85+` |
| S-6 | The Paper Desk snapshot API recomputes unrealized PnL from positions but does NOT pass a live mark price to the portfolio accounting service. The accounting service uses its own logic to derive mark price, which may differ from both engine and UI BTC price feeds. | `client/src/app/api/paper-desk/snapshot/route.ts:43-53` |
| S-7 | `DeskCommandCenter` component makes broker API calls with `.catch(() => {})`. If an order placement fails (broker API down), the UI shows success state and the actual failure is invisible to the user. | `client/src/components/DeskCommandCenter.tsx:176` |
| S-8 | `BTCFuturesScalper.tsx:268` has `.catch(() => {})` — persistence write failure swallowed. A trade that appears to open in the UI may not be written to MongoDB. | `client/src/components/BTCFuturesScalper.tsx:268` |
| S-9 | Paper Desk positions shown in the snapshot are limited to `listOpenPositions` (100 by default). If there are more than 100 open positions, the UI silently shows a truncated list. | `client/src/app/api/paper-desk/positions/route.ts:21` |
| S-10 | `equity_recorder.go` records equity at 1-minute intervals. But the MongoDB `equity_curve` collection uses a TTL of 90 days — older data auto-deleted. If the UI queries for `points=2016` over 90 days, it may receive sparse data without warning. | `engine/internal/paperpersist/equity_recorder.go:6`, `engine/internal/paperpersist/mongo.go:210` |
| S-11 | `fetchStrategyHealth()` in `usePaperDesk.ts` is called once on `desk.connection === "live"` and on `lastUpdated` change. If the engine refreshes strategy scores outside this 5s window, the UI may show stale strategy metrics. | `client/src/components/TerminalDashboard.tsx:264-277` |
| S-12 | The root dashboard's "Signal Feed" falls back to `desk.recentTrades` when no OMS orders are returned. This fallback shows closed trade data as if they were current OMS signals — a semantically wrong substitution. | `client/src/components/TerminalDashboard.tsx:350-363` |
| S-13 | No write fence between `paper_state` and `paper_positions` MongoDB writes. The snapshotter and position writer run in separate goroutines. A read that straddles these writes sees a `paper_state.open_position_count = 5` but only 4 positions in `paper_positions`. | `engine/internal/paperpersist/state_snapshotter.go`, `engine/internal/paperpersist/writer.go` |
| S-14 | `portfolioLedger` in the orchestrator accumulates in-memory. `paper_trades` MongoDB stores the canonical history. The engine's in-memory `portfolioLedger` is rebuilt from MongoDB on restart. During the rebuild window after restart, `paper_state` snapshots reflect an incomplete ledger. | `engine/internal/paperpersist/portfolio_ledger.go:37-42`, `engine/internal/paperpersist/recovery.go` |
| S-15 | The mock trading engine (`useMockTradingEngine`) and the Paper Desk hook (`usePaperDesk`) both poll MongoDB collections that may overlap (the mock engine writes `paper_trades` with a different `account_key`). If account keys collide, trades from one system appear in the other system's UI. | `client/src/hooks/useMockTradingEngine.ts:218`, `client/src/lib/authoritativeAccountKey.ts:6` |
| S-16 | `useLiveBTCPrice` and `usePaperDesk` run independently at different intervals (3s vs 5s). The root dashboard displays both simultaneously. A position's unrealized PnL shown in the positions table uses the BTC price from `useLiveBTCPrice` at the render timestamp, not from `usePaperDesk` data. | `client/src/components/TerminalDashboard.tsx:692-710` |
| S-17 | The `AngelOne` order placement (`/api/angelone/order`) is blocked/retired. The NIFTY options engine still calls AngelOne via the Go engine path for real order placement. But the Go engine AngelOne client (`marketdata.AngelOneClient`) and the Next.js AngelOne proxy are completely separate codepaths with no synchronization. | `engine/cmd/antigravity/main.go:81`, `client/src/app/api/angelone/order/route.ts:6` |
| S-18 | The strategy scoring computation uses trades from the last N months (inferred from query). A strategy that was excellent 2 months ago but terrible this week might still show HEALTHY if its historical trades dominate the window. No recency weighting in scoring. | `engine/internal/paperpersist/strategy_health.go:47-100` |
| S-19 | The `paper_orders` collection (`ColPaperOrders`) is written by the order writer for OMS transitions. The UI reads it via `/api/paper-desk/orders`. But the `paper_state.open_position_count` is sourced from the in-memory position manager, not counted from `paper_orders`. These could diverge. | `engine/internal/paperpersist/mongo.go:63`, `engine/internal/trading/paperpersist_hooks.go:122-136` |
| S-20 | `PortfolioLedger.StartingBalance` is set once at construction. If the engine receives a capital injection (i.e., someone manually modifies the paper balance in MongoDB), the in-memory ledger does not reflect it until restart. | `engine/internal/paperpersist/portfolio_ledger.go:37-41` |
| S-21 | Live BTC price `ticksPerSecond` counter in `useLiveBTCPrice` resets every second. In a tab that has been hidden (tab visibility = hidden), the browser throttles setInterval — `tpsInterval` fires less often, causing `ticksPerSecond` to appear as 0 even when price data is actually arriving. | `client/src/hooks/useLiveBTCPrice.ts:76-80` |
| S-22 | The reconciliation engine (`reconciliationv2`) runs and may trigger the kill switch via `CriticalDriftKillSwitchHook`. However, the kill switch state change is not immediately reflected in any MongoDB collection. The UI can show open positions for up to 10s after a kill switch fires. | `engine/internal/reconciliationv2/wiring.go:54` |
| S-23 | `paper_state.snapped_at` is set by the engine to `time.Now()` at snapshot time. The UI reads this field but does not display it. Users have no way to know when the last successful state snapshot was. | `engine/internal/paperpersist/state_snapshotter.go:124`, `client/src/lib/paperDeskClient.ts:47` |
| S-24 | No distributed lock between multiple potential engine instances. If two engine processes start against the same MongoDB (e.g., rolling restart), both would write to the same `paper_state` singleton simultaneously, causing race conditions. | `engine/internal/paperpersist/state_snapshotter.go:161` |
| S-25 | The `strategy_scores` collection is upserted per strategy by the 15-minute health monitor. But the `strategy_health` collection is also upserted. If the health monitor is skipped for one cycle (e.g., high load), scores and health docs can be from different computation times. | `engine/internal/paperpersist/strategy_health.go:105+` |

---

## TOP 25 UPGRADE OPPORTUNITIES (ranked by ROI)

| Rank | Opportunity | Estimated Value | Effort |
|---|---|---|---|
| 1 | Connect terminal WebSocket to engine (or add DEMO DATA banners) | Eliminates user trust violation | 1-3 days |
| 2 | Use engine mark price in portfolio accounting API | Eliminates unrealized PnL discrepancy | 0.5 days |
| 3 | Fix `catch {}` in CommandCenter, BTCFuturesScalper, DeskCommandCenter | Surface silent failures to operators | 1 day |
| 4 | Persist kill switch state to MongoDB (survive restarts) | Trading safety: kill switch now survives restarts | 2 days |
| 5 | Persist OMS v3 ledger events to MongoDB | Full event replay after restart; reconciliation gains history | 3-5 days |
| 6 | Reduce `PNL_TOLERANCE_USD` from $50 to $5 | Better accounting accuracy detection | 1 hour |
| 7 | Add `snapped_at` display in Paper Desk header | Operators know data age | 2 hours |
| 8 | Wire real strategy health counts to ResearchCenter tournament panel | Eliminate 4 hardcoded metrics | 1 day |
| 9 | Write reconciliation audit entries to MongoDB | Operators can see all drift events | 2-3 days |
| 10 | Replace hardcoded correlation heatmap with real strategy correlations | Meaningful risk management | 2 days |
| 11 | Add "stale for X minutes" timeout to Paper Desk stale state | Prevent silent data staleness | 0.5 days |
| 12 | Implement strategy enable/disable via admin API | Live strategy management without redeploy | 3-5 days |
| 13 | Wire Trade Journal to real paper trades | Journal becomes an operational audit tool | 2 days |
| 14 | Expose execution watchdog status in snapshot API | Detect stalled engines earlier | 1 day |
| 15 | Add live mark price to Paper Desk snapshot response | Position PnL consistency across UI and engine | 0.5 days |
| 16 | Consolidate `initialPaperBalanceUSD`/`PAPER_DESK_STARTING_BALANCE` into single constant | Eliminate silent accounting divergence | 2 hours |
| 17 | Write portfolio risk metrics (VaR/CVaR) to MongoDB from engine | Enable real-time risk dashboard | 5-7 days |
| 18 | Add exponential backoff to `usePaperDesk` polling | Reduce Vercel function invocations on persistent errors | 1 day |
| 19 | Migrate `/btc-future-trading` client-side engine to use `/paper-desk` Go engine | Single source of truth for all trading | 2-3 weeks |
| 20 | Add recency weighting to strategy scoring | CRITICAL for detecting regime-shifted strategies | 3 days |
| 21 | Remove `loadDotEnv` `.env` file loading from repo root | Security: credentials should not be in repo root | 1 hour |
| 22 | Add `portfolio_risk` event log to MongoDB | Enables historical risk audit trail | 3-4 days |
| 23 | Add `snapped_at` TTL warning for stale `paper_state` | Detect engine crashes without monitoring | 1 day |
| 24 | Add session gate verification for all Indian market strategies globally | Prevent orders outside NSE hours | 2 days |
| 25 | Implement distributed lock for engine startup (prevent dual-engine writes) | Data integrity in rolling restarts | 3 days |
