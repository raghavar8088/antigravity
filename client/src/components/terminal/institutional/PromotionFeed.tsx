"use client";

/** @deprecated Promotion pipeline removed. */
export function PromotionFeed({
  maxHeight = "max-h-96",
  compact = false,
}: {
  maxHeight?: string;
  compact?: boolean;
}) {
  void maxHeight;
  void compact;
  return null;
}
