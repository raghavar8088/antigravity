Backtest-to-Live Pipeline Report
1. Backtest engine location & entrypoints

Real engine: engine/internal/backtest/ (batch runner, promotion.go, plus internal/backtest/v2/ for portfolio/regime backtests, internal/backtest/v3/backtest_validation.go for stress-test replay).
Real CLI entrypoint: engine/cmd/run_backtest/main.go. Flags: --symbol, --from, --to, --cache-dir (default data/historical), --out, --hw-only. Loads OHLCV via marketdata.NewHistoricalLoader, builds a multi-timeframe dataset (BuildMTFDataset), filters strategies to OHLCVCompatible ones (line 216), runs backtest.NewBatchRunner(ds, cfg, allEntries).RunAll(...) (line 228-237), then calls backtest.EvaluatePromotion(&results[i], nil) per strategy (line 244) and writes ranked JSON results.
engine/cmd/backtest/main.go is a stale toy/demo (mock sine-wave data, strategy.NewMovingAverageCrossover) — not the real pipeline, ignore it.
Companion loader: engine/cmd/fetch_historical (referenced in run_backtest's error message, line 60) downloads/caches candles into data/historical.
2. Strategy registration

engine/internal/strategy/scalpers/curated_registry.go. Strategies are added via builder functions (BuildAllScalpers, BuildPortedStrategies, buildExpansionPack, buildVolatilityFamily, buildMicrostructureFamily, buildMacroFamily, buildStatisticalFamily, buildEventFamily, buildFamily1Momentum...buildFamily5DerivativesMacro, buildImmortalEditionPack) that each return []RegistryEntry{Name, Strategy, OHLCVCompatible, ...}.
BuildCuratedScalpers() (lines 91-148) aggregates all builders, wraps non-phase-gated strategies with withShadowOverride() (line 140), then applies filterTradeEngineEnabled() (line 143, defined 71-86) — the whitelist gate — followed by FilterWinnersOnly() (lines 153-175, exported) which drops strategies with ≥30 trades and Active=false.
The live/pre-live whitelist itself: tradeEngineEnabled map, lines 45-67, dated "Selected from the Strategy Leadership Board (composite score) on 2026-07-03" — 21 strategies (matches memory note, roughly).
Consumed by engine/cmd/antigravity/main.go:523 — strategy.BuildCuratedScalpers() is what the live/pre-live engine actually runs.
3. engine/data/backtest_*.json schema
All share one schema: {elapsed_s, from, ran_at, strategies: [{rank, strategy, total_trades, win_rate_pct, sharpe, max_drawdown_pct, profit_factor, total_return_pct, avg_win_pct, avg_loss_pct, meets_promotion, demote_reason?}]} — this is the direct JSON serialization of ResultRow (run_backtest/main.go lines 17-30). backtest_baseline.json (from 2021-01-01, ran 2026-06-26) and backtest_hw_v2..v6.json/backtest_hw_strategies.json are successive --hw-only runs adding new hand-written (HW) strategy batches (the hwNames map, lines 74-214, shows the full HW1–HW245 lineage). backtest_results_prelive.json (from 2023-01-01, ran 2026-07-02) is the most recent full run feeding the pre-live promotion decision. backtest_results_v2..v6.json are versioned full-universe runs.

4. Historical data & run mechanics

engine/data/historical/: raw cached OHLCV JSON per timeframe — BTCUSDT_{1m,5m,15m,1h,4h,1d}.json plus BTCUSDT_funding.json. Populated by cmd/fetch_historical, consumed by marketdata.HistoricalLoader.BuildMTFDataset.
engine/data/alpha/funding.ndjson: supplementary funding-rate alpha feed (newline-delimited JSON), separate from the OI/funding already in data/historical.
No Makefile/README found automating this — the run is manual: go run ./cmd/fetch_historical then go run ./cmd/run_backtest --from ... --to ... --out .... engine/run_backtest.exe is a prebuilt binary of the same command.
5. Backtested → qualified → whitelisted pipeline

run_backtest runs all OHLCVCompatible strategies over history, computes metrics, calls EvaluatePromotion per result.
EvaluatePromotion (engine/internal/backtest/promotion.go:39-65) checks against PromotionCriteria (lines 12-26): MinSharpe 1.0, MinWinRate 45%, MaxDrawdownPct 20%, MinProfitFactor 1.3, MinTrades 50, MinStressPasses 3 (stress check skipped when v3Result is nil, as in the CLI run) — sets sr.MeetsPromotion=true only if all pass.
PromoteFromResult(sr) (lines 67+) writes a live Performance{Active:true} record into the global performanceRegistry via scalers.UpdatePerformance — this is the actual "qualification" action (not automatic; something/someone must call it after reviewing meets_promotion in the output JSON).
Separately, tradeEngineEnabled (curated_registry.go:45-67) is a manually-curated, hardcoded whitelist ("operator-selected... from the Strategy Leadership Board") applied in filterTradeEngineEnabled — this is the final human-in-the-loop gate that decides which of the "qualified" strategies actually run in the live/pre-live trade engine, independent of the Performance.Active flag from promotion. Both gates (filterTradeEngineEnabled then FilterWinnersOnly) are applied in sequence inside BuildCuratedScalpers().
DemotionCriteria (lines 29-35, MinTrades 30, MaxWinRate 40%) provides auto-demotion once a strategy has live trade history and underperforms, flipping Active=false, which FilterWinnersOnly then excludes.
Note: I did not find explicit code tying "100 qualified strategies" to the whitelist count (whitelist currently has 21 names) — that gap between backtest-qualified pool and the smaller live whitelist appears to be a manual operator curation step, not an automated pipeline stage.

Backtest → Qualified → Live Pipeline
Backtest engine

Real entrypoint: engine/cmd/run_backtest/main.go — flags --symbol --from --to --cache-dir --out --hw-only. Loads OHLCV via marketdata.NewHistoricalLoader, builds multi-timeframe data, filters to OHLCVCompatible strategies, runs backtest.NewBatchRunner(...).RunAll(...), then backtest.EvaluatePromotion per result, writes ranked JSON.
Engine core: engine/internal/backtest/ (promotion.go, v2/ portfolio/regime tests, v3/backtest_validation.go stress replay).
⚠️ engine/cmd/backtest/main.go is a stale toy demo (sine-wave mock data) — not the real pipeline.
Historical data lives in engine/data/historical/BTCUSDT_{1m,5m,15m,1h,4h,1d,funding}.json, fetched via engine/cmd/fetch_historical. No Makefile — it's run manually: go run ./cmd/fetch_historical then go run ./cmd/run_backtest --from ... --to ... --out ....
Strategy registration — strategy/curated_registry.go

Builder functions (BuildAllScalpers, buildExpansionPack, buildVolatilityFamily, etc.) each return []RegistryEntry.
BuildCuratedScalpers() (lines 91-148) aggregates all builders → applies filterTradeEngineEnabled() (the whitelist gate, line 143) → applies FilterWinnersOnly() (drops ≥30-trade losers, lines 153-175).
tradeEngineEnabled map (lines 45-67): 21 hardcoded, operator-curated strategy names, dated 2026-07-03 "Selected from the Strategy Leadership Board."
Consumed live by engine/cmd/antigravity/main.go:523.
JSON result files (engine/data/backtest_*.json)
All share one schema (ResultRow): {elapsed_s, from, ran_at, strategies:[{rank, strategy, total_trades, win_rate_pct, sharpe, max_drawdown_pct, profit_factor, total_return_pct, meets_promotion, demote_reason?}]}

backtest_baseline.json — full run from 2021-01-01
backtest_hw_v2..v6.json / backtest_hw_strategies.json — successive --hw-only runs adding hand-written strategy batches (HW1–HW245 lineage)
backtest_results_prelive.json — most recent full run (from 2023-01-01, ran 2026-07-02), feeds the pre-live decision
backtest_results_v2..v5.json — versioned full-universe runs
Qualification pipeline

EvaluatePromotion (promotion.go:39-65) checks PromotionCriteria (lines 12-26): Sharpe ≥1.0, win rate ≥45%, max drawdown ≤20%, profit factor ≥1.3, trades ≥50, stress passes ≥3 (skipped when no v3 stress result). Sets meets_promotion=true if all pass.
PromoteFromResult writes Performance{Active:true} into the global registry via scalers.UpdatePerformance — this is what makes a strategy "qualified." It's not automatic; someone reviews the output JSON and calls it.
Separately, tradeEngineEnabled is a manual, hardcoded whitelist — the final human gate deciding which qualified strategies actually run live, independent of the Active flag.
DemotionCriteria (lines 29-35: <30 trades, win rate <40%) auto-flips Active=false once live trade history accumulates, and FilterWinnersOnly then drops them.
Gap found: there's no code tying "100 qualified strategies" to a specific list — the whitelist currently holds 21 names. The step from "100 backtest-qualified" down to "21 live-whitelisted" is manual operator curation, not an automated pipeline stage.

What would you like to do next — add new strategies to the registry, re-run a backtest, or expand the live whitelist?

CREATE BACKTEST.MD FILE STORE THIS DETAILES IN THAT FILE , SO NEXT TIME IF I TELL YOU LIKE ADD 10 MORE STATERGIES AND DO BACKTEST AND TELL ME WHRETHER THOS STATERGIES ARE QUALIFIED OR NOT , YOU SHPULD FALLOW THIS STEPS WITHOUT MISS.
Bash
IN
ls "d:/APPLICATION/antigravity-main/antigravity-main" | head -30

OUT
AI_BOT_SETUP_INSTRUCTIONS.txt
AI_CONTEXT.md
AWS-REDEPLOY.md
BOT_FILE_MANIFEST.txt
BTC_PILOT_SOVEREIGN_HANDOFF.md
CHANGELOG.md
CLAUDE.md
DEPLOY.md
GO_LIVE_RUNBOOK.md
LightsailDefaultKey-ap-south-1.pem
README.md
REDEPLOY.md
__pycache__
agent-config.json
client
client_start_stderr.log
client_start_stdout.log
client_verify_stderr.log
client_verify_stdout.log
data
docker-compose.prod.yml
docs
engine
engine_conservative_stderr.log
engine_conservative_stdout.log
engine_restart.log
engine_restart_err.log
engine_smoke_stderr.log
engine_smoke_stdout.log
engine_start.log
I'll write it at the project root, alongside the other operational docs.

Write BACKTEST.md
110 lines
# Backtest → Qualification → Live Pipeline (Reference Procedure)

This document is the authoritative, step-by-step procedure for adding new strategies,
backtesting them, and determining whether they qualify for the pre-live/live trade
engine. Follow these steps exactly whenever asked to "add N strategies and backtest them."

## 1. Architecture map

| Purpose | Path |
|---|---|
| Real backtest CLI entrypoint | `engine/cmd/run_backtest/main.go` |
| Backtest engine core | `engine/internal/backtest/` (`promotion.go`, `v2/` portfolio/regime, `v3/backtest_validation.go` stress replay) |
| Historical data fetcher | `engine/cmd/fetch_historical` |
| Historical cached OHLCV data | `engine/data/historical/BTCUSDT_{1m,5m,15m,1h,4h,1d,funding}.json` |
| Alpha/funding feed | `engine/data/alpha/funding.ndjson` |
| Strategy registry (where strategies are added) | `engine/internal/strategy/scalpers/curated_registry.go` |
| Live/pre-live engine consumer | `engine/cmd/antigravity/main.go:523` (`strategy.BuildCuratedScalpers()`) |
| Result output files | `engine/data/backtest_*.json`, `engine/data/backtest_results_*.json` |

⚠️ `engine/cmd/backtest/main.go` is a **stale toy/demo** (mock sine-wave data). Never use it — always use `engine/cmd/run_backtest/main.go`.

## 2. Adding new strategies

1. Implement the strategy satisfying the `OHLCVCompatible` strategy interface used by the registry builders.
2. Add it inside one of the existing builder functions in `curated_registry.go`
   (`BuildAllScalpers`, `BuildPortedStrategies`, `buildExpansionPack`,
   `buildVolatilityFamily`, `buildMicrostructureFamily`, `buildMacroFamily`,
   `buildStatisticalFamily`, `buildEventFamily`, `buildFamily1Momentum` … `buildFamily5DerivativesMacro`,
   `buildImmortalEditionPack`), or create a new builder function following the same
   pattern and wire it into `BuildCuratedScalpers()` (lines ~91-148).
3. Each builder returns `[]RegistryEntry{Name, Strategy, OHLCVCompatible, ...}`.
4. Do **NOT** add the new strategy names to `tradeEngineEnabled` (lines ~45-67) yet —
   that whitelist is the final manual gate, applied only *after* backtest qualification (step 5).

## 3. Fetch/verify historical data (only if date range not already cached)

```
go run ./cmd/fetch_historical
```
This populates/refreshes `engine/data/historical/`. Skip if the required symbol/date range is already cached.

## 4. Run the backtest

```
go run ./cmd/run_backtest --symbol BTCUSDT --from <start-date> --to <end-date> --cache-dir data/historical --out data/backtest_results_<label>.json
```
- Use `--hw-only` if only testing hand-written strategies added since the last full run.
- Name the `--out` file descriptively (e.g. `backtest_results_v7.json`) — never overwrite `backtest_results_prelive.json` directly; that file is the canonical "last decision" snapshot.
- The runner automatically restricts to `OHLCVCompatible` strategies and calls `EvaluatePromotion` per strategy.

## 5. Read qualification results

Output schema (`ResultRow`), one entry per strategy:
```
{
  "elapsed_s": ..., "from": ..., "ran_at": ...,
  "strategies": [
    {
      "rank": ..., "strategy": "...",
      "total_trades": ..., "win_rate_pct": ..., "sharpe": ...,
      "max_drawdown_pct": ..., "profit_factor": ..., "total_return_pct": ...,
      "avg_win_pct": ..., "avg_loss_pct": ...,
      "meets_promotion": true/false,
      "demote_reason": "..."   // present only if failed
    }
  ]
}
```

**Promotion criteria** (`engine/internal/backtest/promotion.go:12-26`) — a strategy is "qualified" (`meets_promotion: true`) only if ALL of:
- Sharpe ≥ 1.0
- Win rate ≥ 45%
- Max drawdown ≤ 20%
- Profit factor ≥ 1.3
- Total trades ≥ 50
- Stress passes ≥ 3 (skipped/not enforced when no v3 stress-test result is attached — the plain CLI run does not attach one)

For each newly added strategy, report: pass/fail against each individual criterion, not just the final boolean — the user wants to know *why* a strategy failed, not just that it did.

## 6. Promote qualified strategies

Strategies with `meets_promotion: true` are candidates for `PromoteFromResult()`
(`promotion.go:67+`), which writes `Performance{Active:true}` into the global
performance registry via `scalers.UpdatePerformance`. This is **not automatic** —
it must be explicitly invoked/reviewed per strategy after inspecting the backtest output.

## 7. Add to the live trade-engine whitelist (final manual gate)

Only after a strategy is qualified (step 5) AND promoted (step 6) should its name be
added to the `tradeEngineEnabled` map in `curated_registry.go:45-67`. This is a
manually curated whitelist ("Selected from the Strategy Leadership Board") — it is
the actual gate for what runs in the live/pre-live engine, independent of the
`Active` flag from promotion. Update the comment/date on the map when adding entries.

Sequence enforced in `BuildCuratedScalpers()`: all builders → `filterTradeEngineEnabled()` → `FilterWinnersOnly()` (drops strategies with ≥30 trades and `Active=false`).

## 8. Demotion (ongoing, automatic once live)

`DemotionCriteria` (`promotion.go:29-35`): trades ≥ 30 and win rate < 40% → `Active` flips
to `false` → `FilterWinnersOnly()` excludes it from future `BuildCuratedScalpers()` calls.
No action needed here unless investigating a live regression.

## Known gap (as of 2026-07-03)

There is no automated code path from "100 backtest-qualified strategies" to the
21-name `tradeEngineEnabled` whitelist — narrowing the qualified pool down to the
live whitelist is a manual operator decision, not a pipeline stage. When asked to
"add N strategies to the live engine," steps 6 and 7 must be done explicitly, one
strategy at a time, based on reviewed backtest output.
Created BACKTEST.md at the project root with the full 8-step procedure: registry map, how to add strategies to curated_registry.go, fetch/run backtest commands, promotion criteria with exact thresholds, promotion mechanism, and the manual whitelist gate. I'll save a memory pointer so future sessions know to consult it.