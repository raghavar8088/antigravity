apiVersion: v1
kind: Service
metadata:
  name: btc-engine
  namespace: btc-pilot
  labels:
    app: btc-engine
spec:
  type: ClusterIP
  selector:
    app: btc-engine
  ports:
    - port: 8080
      targetPort: 8080
      name: http
      protocol: TCP
