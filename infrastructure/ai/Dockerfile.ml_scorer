FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn[standard] xgboost numpy pydantic

COPY ml_scorer.py .

EXPOSE 8002

CMD ["uvicorn", "ml_scorer:app", "--host", "0.0.0.0", "--port", "8002"]
